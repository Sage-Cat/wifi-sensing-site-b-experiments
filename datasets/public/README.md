# Public bundles

This directory intentionally contains no bundles. Future bundles must be
explicitly allowlisted by `../manifest.json` and include their required public
documentation, metadata, and checksum files.
